# Required libraries — smart-fall-detection-bracelet
# Install via Arduino IDE: Sketch → Include Library → Manage Libraries

# ── Native to the ESP32 (already included with the Espressif package) ──
Wire.h       # I2C communication with the MPU6050
WiFi.h       # WiFi connection

# ── Install manually ─────────────────────────────────────────────────
PubSubClient # MQTT client — author: Nick O'Leary
             # search for "PubSubClient" in the Library Manager
             # recommended version: 2.8 or higher

# ── ESP32 package (Boards Manager) ───────────────────────────────────
# URL: https://raw.githubusercontent.com/espressif/arduino-esp32/gh-pages/package_esp32_index.json
# Package: esp32 by Espressif Systems
# Tested version: 2.x
# Selected board: ESP32 Dev Module
